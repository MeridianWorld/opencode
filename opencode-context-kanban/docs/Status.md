# 当前实现状态 (Implementation Status)

本文档用于把“代码是否已实现”和“2026-04-04 这轮现场是否已验证”区分开写，避免文档状态和真实行为脱节。

## 现场结论

- 代码侧已完成问题 1 到问题 6 的修复。
- 文档侧正在对齐问题 8，本文件即为本轮对齐产物。
- 当前现场环境里，上游 `http://localhost:4096/event` 仍不可达，因此真实上游事件流相关能力暂时只能验证“断连提示”和“降级反馈”，不能算完整现场验收通过。

## 近期修复概览

- [x] **上游 SSE 状态可见性**：增加前后端连接异常捕捉、重连尝试及显式用户提示，避免由于无数据造成的误解（对应问题 1、7）。
- [x] **message.removed 前端状态处理**：补全 `message.removed` 和 `message.part.removed` 状态逻辑，事件发生时同步更新 Token 统计与视图（对应问题 2）。
- [x] **session.compacted 差异视图**：补充会话级压缩的 Diff 弹窗渲染能力（对应问题 3）。
- [x] **响应式与移动端溢出修复**：侧边栏和主栏目在移动设备上响应式堆叠，避免水平滚动条（对应问题 4）。
- [x] **图表初始化告警修复**：改为在容器宽度可测量后再绘制图表，清除 `width(-1)` / `height(-1)` 告警（对应问题 5）。
- [x] **WebSocket 重复连接修复**：在 React StrictMode 下增加单例保护机制，阻止重复订阅 WS（对应问题 6）。

## 按能力划分的当前状态

### 已实现且已验证

- Session 列表渲染与切换
- `message.removed` / `message.part.removed` 的状态同步
- `session.compacted` 的差异视图基础展示
- Token 统计与图表稳定渲染
- 移动端 `390px` 视口下无横向溢出
- React StrictMode 下避免重复 WebSocket 连接
- 上游断连时的状态提示

### 已实现但现场仍受阻

- 真实上游 SSE 事件流接入
- 基于真实上游数据的实时 Timeline、Message、Token 连续更新

阻塞原因：本轮现场测试期间，上游 `4096` 事件流不可达，导致只能验证前端和后端的降级处理，不能完成真实在线流的端到端验收。

### 仍待完善

- 更细粒度的 Prune / Tool Output 截断 Diff
- 长时间运行和高并发场景下的数据一致性回归验证
- 多会话高级搜索、过滤和历史持久化能力
